import { Layout } from 'antd';
import * as React from 'react';

const { Footer } = Layout

class FooterComponent extends React.Component {
    public render() {
        return <Footer className="footer">Hello</Footer>
    }
}


export default FooterComponent;